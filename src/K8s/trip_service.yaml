apiVersion: v1
kind: Service
metadata:
  name: trip-service
  namespace: api-dev
  labels:
    run: trip-label
spec:
  selector:
    run: trip-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP